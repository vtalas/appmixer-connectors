{
    "name": "appmixer.getResponse",
    "version": "1.1.0",
    "changelog": {
        "1.1.0": [
            "Fix UpdateCampaign, UpdateContact, UpdateTag to use PATCH instead of POST",
            "Fix CreateTag response mapping (tagId field)",
            "Fix FindTags output schema field name (tagId)",
            "Fix FindCampaigns output schema field name (campaignId)",
            "Add output schemas to UpdateCampaign and UpdateContact",
            "Fix auth.js API key profile mask",
            "Remove inconsistent quota scope from FindContacts",
            "Add private:false to CreateTag",
            "Update service.json description",
            "Add E2E test flows"
        ],
        "1.0.0": [
            "Initial version"
        ]
    }
}