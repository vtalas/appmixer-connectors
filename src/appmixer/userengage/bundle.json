{
    "name": "appmixer.userengage",
    "version": "1.2.1",
    "changelog": {
        "1.0.1": [
            "Initial version"
        ],
        "1.1.0": [
            "add MakeApiCall component with key-value array inputs for headers and parameters per standard #1459"
        ],
        "1.2.0": [
            "Added test() (Flow Test Mode) to all triggers: NewConversation, NewConversationMessage, NewEvent, NewTemplate, NewUser, SyncSource"
        ],
        "1.2.1": [
            "Output examples for every field of the conversation, event, template, tag and user components. SyncSource's fields point at `data.*`, where the user is emitted (they resolved to nothing before), and `phase` / `source` are declared. FindByEmail read the email from a port that does not exist and now reads its `in` input."
        ]
    }
}
